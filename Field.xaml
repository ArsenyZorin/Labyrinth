<UserControl x:Class="TestTaskMitsar.Field"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestTaskMitsar"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="0,272,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="31*"/>
            <ColumnDefinition Width="69*"/>
        </Grid.ColumnDefinitions>
        <Canvas
            Name= "canvasfield"
            SizeChanged="canvasfield_SizeChanged"
            MouseLeftButtonDown="Canvasfield_OnMouseLeftButtonDown" Margin="0,-276,0,0" Grid.ColumnSpan="2"
            />
    </Grid>
</UserControl>
